pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // NewPipeExtractor (YouTube stream/playlist extraction) is published here only.
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "NSLDownloader"
include ':app'
